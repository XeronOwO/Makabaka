namespace Makabaka.Models
{
	/// <summary>
	/// 私聊消息发送者信息<br/>
	/// 需要注意的是，sender 中的各字段是尽最大努力提供的，也就是说，不保证每个字段都一定存在，也不保证存在的字段都是完全正确的（缓存可能过期）。
	/// </summary>
	public class PrivateMessageSenderInfo : MessageSenderInfo
	{
	}
}
